package com.hellguy39.data.db
/*

import androidx.room.*
import com.hellguy39.data.models.TrackDatabase

@Dao
interface TracksDao {

    @Query("SELECT * FROM TrackDatabase")
    suspend fun getAllTracks(): List<TrackDatabase>

    @Insert(onConflict = OnConflictStrategy.REPLACE)
    suspend fun insertTrack(track: TrackDatabase)

    @Delete
    suspend fun deleteTrack(track: TrackDatabase)
}*/
